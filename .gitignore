# Ignore all files in the prometheus/data folder
prometheus/data/*

# But don't ignore the .gitkeep file
!prometheus/data/.gitkeep

# Ignore all ini files in the grafana folder
grafana/*.ini

# Add any example files for grafana but not ini files
!grafana/*.example
